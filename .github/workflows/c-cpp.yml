name: C++ Build test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: git init
    - run: git remote add origin https://github.com/FloatingComet62/sdl_cdn
    - run: git fetch
    - run: git reset origin/master
    - run: git checkout -t origin/master
    # - name: configure
    #   run: ./configure
    - name: make
      run: make
    # - name: make check
    #   run: make check
    # - name: make distcheck
    #   run: make distcheck
